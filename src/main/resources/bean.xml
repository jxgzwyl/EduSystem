<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
       http://www.springframework.org/schema/context  http://www.springframework.org/schema/context/spring-context-3.2.xsd
       http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd"
        >
    <!-- 配置指定扫描的包 -->
    <context:component-scan base-package="com.zikool.edu"/>
    <!-- 配置 JdbcTemplate 实例 -->
    <!-- 配置 proxool 数据源 -->

<!--    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName">
            <value>org.logicalcobwebs.proxool.ProxoolDriver</value>
        </property>
        <property name="url">
            <value>proxool.edu</value>
        </property>
    </bean>-->
    <bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    <!-- 启用基于注解的事务管理 -->
    <tx:annotation-driven transaction-manager="txManager"/>
    <bean id="jdbcTemplate"
          class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    <context:property-placeholder location="classpath:proxool.properties"/>
  <bean id="dataSource"
            class="org.logicalcobwebs.proxool.ProxoolDataSource">
          <property name="driver">
              <value>${jdbc-0.proxool.driver-class}</value>
          </property>
          <property name="driverUrl">
              <value>${jdbc-0.proxool.driver-url}</value>
          </property>
          <property name="user">
              <value>${jdbc-0.user}</value>
          </property>
          <property name="password">
              <value>${jdbc-0.password}</value>
          </property>
          <property name="alias">
              <value>${jdbc-0.proxool.alias}</value>
          </property>
          <property name="houseKeepingTestSql">
              <value>${jdbc-0.proxool.house-keeping-test-sql}</value>
          </property>
          <property name="simultaneousBuildThrottle">
              <value>${jdbc-0.proxool.simultaneous-build-throttle}</value>
          </property>
          <property name="prototypeCount">
              <value>${jdbc-0.proxool.prototype-count}</value>
          </property>
          <property name="maximumConnectionCount">
              <value>${jdbc-0.proxool.maximum-connection-count}</value>
          </property>
          <property name="minimumConnectionCount">
              <value>${jdbc-0.proxool.minimum-connection-count}</value>
          </property>
          <property name="trace">
              <value>${jdbc-0.proxool.trace}</value>
          </property>
          <property name="verbose">
              <value>${jdbc-0.proxool.verbose}</value>
          </property>
      </bean>


</beans>